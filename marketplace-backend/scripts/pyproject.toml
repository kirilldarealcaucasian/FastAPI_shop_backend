[project]
name = "scripts"
version = "0.1.0"
description = "Here are stored notebooks for eda, recommendation engine"
readme = "README.md"
requires-python = ">=3.12.8"
dependencies = [
    "kagglehub>=1.0.0",
    "matplotlib>=3.10.8",
    "pandas>=3.0.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
